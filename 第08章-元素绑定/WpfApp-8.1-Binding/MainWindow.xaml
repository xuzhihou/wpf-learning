<Window x:Class="WpfApp_8._1_Binding.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp_8._1_Binding"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <FontFamily x:Key="CustonFont">Calibri</FontFamily>
    </Window.Resources>
    <StackPanel>
        <Slider Name="SliderFontSize" Margin="3" Minimum="1" Maximum="40"
                Value="{Binding ElementName=txtFontSize,Path=Text,Mode=OneWayToSource,Delay=500}" 
                TickFrequency="1" TickPlacement="TopLeft" IsSnapToTickEnabled="True"></Slider>
        <TextBlock Margin="10" Text="Simple Text" Name="lblSampleText" FontSize="{Binding ElementName=SliderFontSize,Path=Value}"></TextBlock>
        <StackPanel Orientation="Horizontal">
            <Button Margin="10" Content="Send to Small" Click="cmd_SetSmall"></Button>
            <Button Margin="10" Content="Send to Normal" Click="cmd_SetNormal"></Button>
            <Button Margin="10" Content="Send to Large" Click="cmd_SetLarge"></Button>
        </StackPanel>
        <TextBox Margin="10" Name="txtFontSize" Text="{Binding ElementName=SliderFontSize,Path=Value, Mode=OneWayToSource}"
                 ></TextBox>
        <Button Content="hello"></Button>
        <TextBlock Text="{Binding Source={x:Static SystemFonts.IconFontFamily},Path=Source}"></TextBlock>
        <TextBlock Text="{Binding Source={StaticResource CustonFont},Path=Source}"></TextBlock>

        <!--<TextBlock>
            <TextBlock.Text>
                <Binding Path="Title">
                    <Binding.RelativeSource>
                        <RelativeSource Mode="FindAncestor" AncestorType="{x:Type Window}"></RelativeSource>
                    </Binding.RelativeSource>
                </Binding>
            </TextBlock.Text>
        </TextBlock>-->
        <TextBlock Text="{Binding Path=Title,RelativeSource={RelativeSource FindAncestor,AncestorType={x:Type Window}}}"></TextBlock>
        <TextBlock Text="DataContext如下"></TextBlock>
        
        <StackPanel DataContext="{x:Static SystemFonts.IconFontFamily}">
            <TextBlock Margin="5" Text="{Binding Path=Source}"></TextBlock>
            <TextBlock Margin="5" Text="{Binding Path=LineSpacing}"></TextBlock>
            <TextBlock Margin="5" Text="{Binding Path=FamilyTypefaces[0].Style}"></TextBlock>
        </StackPanel>
    </StackPanel>
    
</Window>
